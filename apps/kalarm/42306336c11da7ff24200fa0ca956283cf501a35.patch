From 42306336c11da7ff24200fa0ca956283cf501a35 Mon Sep 17 00:00:00 2001
From: Daniil-Viktor Ratkin <krf10@altlinux.org>
Date: Wed, 29 Nov 2023 10:58:45 +0300
Subject: [PATCH] show correct calendar names when deleting

---
 src/resourceselector.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/resourceselector.cpp b/src/resourceselector.cpp
index 0420d5360..dbb25ebbf 100644
--- a/src/resourceselector.cpp
+++ b/src/resourceselector.cpp
@@ -218,7 +218,7 @@ void ResourceSelector::removeResource()
     if (!resource.isValid())
         return;
     qCDebug(KALARM_LOG) << "ResourceSelector::removeResource:" << resource.displayName();
-    const QString name = resource.configName();
+    const QString name = resource.displayName();
     // Check if it's the standard or only resource for at least one type.
     const CalEvent::Types allTypes      = resource.alarmTypes();
     const CalEvent::Types standardTypes = Resources::standardTypes(resource, true);
-- 
GitLab

